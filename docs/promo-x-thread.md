# X / Twitter promo — ai-browser-mcp

Ready to post. The thread works well with a 15–30s screen-capture GIF showing an agent searching
Zhihu / Xiaohongshu / X in a background Chrome window.

## Full thread (post in order)

**1 — hook**
```
Your AI's search is broken — it can't read the page *you're* logged into.

I built ai-browser-mcp: an MCP server that lets any agent search Zhihu, X, Xiaohongshu, Douyin & Bilibili using YOUR real Chrome login. No headless, no scraping, no cookie theft.
```

**2 — how it works**
```
It drives a background Chrome window with your real session. The site sees the user — indistinguishable from a human. Logged-in content finally reachable. And it never returns or persists your cookies. 100% local (localhost).
```

**3 — automate + robust**
```
Not just read: click, scroll, type, select, download (SHA-256 verified), full comments with resumable sessions. Because it drives the real UI, site redesigns don't break it — no reverse-engineered APIs.
```

**4 — all agents + compliant**
```
Runs fully compliant & locally, works with any MCP client — Claude Code, Codex, Cursor, Claude Desktop. Open source on GitHub: github.com/ywleeo/browser-mcp
```

**5 — CTA**
```
pip install ai-browser-mcp

Star the repo if you want agents that can genuinely reach logged-in content. 🚀
#MCP #opensource #AI #Claude #Cursor
```

## Short single post (one tweet)
```
Your AI's search is broken — it can't read the page you're logged into. ai-browser-mcp is an MCP server that lets any agent search Zhihu, X, Xiaohongshu, Douyin & Bilibili with YOUR real Chrome login — 100% local, no headless, no cookie theft.

pip install ai-browser-mcp  ·  github.com/ywleeo/browser-mcp
#MCP #opensource #AI
```

## Notes
- Swap in your actual screenshot/GIF as the media attachment (media dramatically boosts reach on X).
- If you have an English vs Chinese audience split, post the English thread to `@...` and a Chinese
  variant on 即刻/知乎/掘金.
- Keep the 5 tweets as one thread (reply to each prior tweet) rather than separate posts — threads get
  better reach than scattered posts.
